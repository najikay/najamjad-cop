"""Central code-version marker.

Why: guidelines §8.1 require an explicit code version starting at 1.00,
validated against config versions at startup and reported in the Step-0
declaration (book rule 53).
"""

CODE_VERSION = "1.00"
